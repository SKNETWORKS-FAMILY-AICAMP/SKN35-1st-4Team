# common/ui.py 의 공통 CSS(밝은 사이드바 그라데이션, 흰색 카드)가 라이트 테마를 전제로
# 작성되어 있어서, 브라우저가 다크 모드면 사이드바 글씨가 안 보인다.
# 앱 테마를 라이트로 고정해 어떤 환경에서도 디자인대로 보이게 한다.
# (다크 테마를 쓰고 싶으면 이 파일을 지우고 common/ui.py의 CSS를 함께 손봐야 한다.)
[theme]
base = "light"
